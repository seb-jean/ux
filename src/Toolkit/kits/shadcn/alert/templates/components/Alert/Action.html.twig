{# @block content The action element, typically a button or link positioned at the top-right #}
<div
    class="{{ ('absolute top-2 right-2 ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({'data-slot': 'alert-action'}) }}
>
    {%- block content %}{% endblock -%}
</div>

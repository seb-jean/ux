{# @block content The breadcrumb structure, typically a `Breadcrumb:List` #}
<nav {{ attributes.defaults({'data-slot': 'breadcrumb', 'aria-label': 'Breadcrumb'}) }}>
    {%- block content %}{% endblock -%}
</nav>

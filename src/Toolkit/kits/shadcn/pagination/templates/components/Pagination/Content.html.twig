{# @block content The pagination items, typically multiple `Pagination:Item` components #}
<ul
    class="{{ ('flex flex-row items-center gap-1 ' ~ attributes.render('class'))|tailwind_merge }}"
    {{ attributes.defaults({'data-slot': 'pagination-content'}) }}
>
    {%- block content %}{% endblock -%}
</ul>

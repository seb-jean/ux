{%- props label = 'Loading' -%}

{{ ux_icon('lucide:loader-2', {
    class: ('size-4 animate-spin ' ~ attributes.render('class'))|tailwind_merge,
    ...attributes.defaults({
        'data-slot': 'spinner',
        'aria-label': label,
        role: 'status',
    }),
}) }}
